# v2.3.0 升级指南

## 变更文件清单（17个文件）

### 替换文件（覆盖原文件）：
```
backend/app/core/config.py          # 版本号 → 2.3.0
backend/app/core/database.py        # 新增 async_session_factory
backend/app/core/security.py        # 新增 RBAC + require_roles
backend/app/models/models.py        # 新增 AnalysisHistory + AuditLog 表
backend/app/schemas/schemas.py      # 新增多个 schema
backend/app/main.py                 # 挂载 audit 路由
backend/app/api/v1/__init__.py      # 无变化（保险替换）
backend/app/api/v1/analysis.py      # ★核心：异步分析 + 进度 + 历史
backend/app/api/v1/documents.py     # 新增预览API + 重试
backend/app/api/v1/projects.py      # 搜索 + 批量删除
backend/app/api/v1/report.py        # 审计日志
backend/app/api/v1/auth.py          # 审计日志
backend/app/api/v1/risk.py          # 无大改（保险替换）
frontend/index.html                 # ★大量UI增强
```

### 新增文件：
```
backend/app/api/v1/audit.py         # 审计日志 API
backend/app/services/audit.py       # 审计日志服务
backend/tests/test_comprehensive.py # 增强测试套件
```

## 部署步骤

```bash
# 1. 在本地 git 仓库中替换上述文件
# 2. 提交推送
git add -A
git commit -m "v2.3.0: async analysis, progress, preview, config, history, audit, RBAC, batch ops, tests"
git push origin main

# 3. Railway 自动部署（2-3分钟）
# 4. 首次启动会自动创建新表（AnalysisHistory, AuditLog）
```

## 新功能概览

| # | 功能 | 说明 |
|---|------|------|
| 1 | 异步分析+进度条 | 分析任务后台执行，前端实时显示进度百分比和当前步骤 |
| 2 | 文档预览 | 点击"👁 预览"查看文档全文和元数据，无需下载原文件 |
| 3 | 分析历史 | 每次分析保留版本记录，支持查看和对比历史版本结果 |
| 4 | 批量操作 | 项目管理支持全选/批量删除，带搜索和风险等级筛选 |
| 5 | 检测参数配置 | 每个项目可独立配置阈值、启用/禁用特定检测维度 |
| 6 | 风险雷达图 | 分析详情页展示七维度雷达图（已有，保持） |
| 7 | 操作日志 | 新增"📋 操作日志"页，记录登录/上传/分析/导出等操作 |
| 8 | RBAC权限 | admin/analyst/auditor 三种角色，审计日志仅admin/auditor可查 |
| 9 | 解析重试 | 文档解析失败自动重试一次 |
| 10 | 错误恢复 | 分析中途报错不会卡在"analyzing"状态 |
| 11 | 综合测试 | 新增 test_comprehensive.py 覆盖所有检测算法 |

## 注意事项

- 数据库自动迁移：新表会自动创建，无需手动操作
- 旧数据兼容：现有项目/文档/分析结果完全保留
- 前端版本标记更新为 v2.3
